# ---- 运行产物（随时可删）----
worktmp/
output/
*.pyc
__pycache__/
.pytest_cache/

# ---- 本地私有设置（不入库）----
.local/
# `config.json` 是**这台机器上**的调参 + 绝对路径（`key_model: D:\...\keys.onnx`）——
# 入库会让别人（以及你换机器之后）拿到一堆只对本机有意义的值。
# 内置默认值在 `a9route/config.py` 的 `DEFAULTS` 里；
# 网页「参数」弹窗就是改这个文件（保存即写盘），要留档就单独存一份 ✓
config.json
# 但**例子文件**要入库：`config.example.json` = 这台机器上实测调好的那几项
# （不含绝对路径），谁都能抄一份成 config.json ✓
!config.example.json
# 测试/探针的临时配置（`tests/__init__.py` 现在会写到系统临时目录或 worktmp/，
# 这条只是兜底：早期版本曾把它掉在仓库根目录上）
a9route-tests-config.json
probe_fake*.mp4
probe_*_shots/

# ---- 数据集与模型 ----
# datasets/ 里有人工复核过的标注（最贵的东西）—— 体积大，不入库，只留一个 README 占位。
datasets/*
!datasets/README.md

# 模型分**两类**，处理方式不同：
#
#  · **运行时模型入库**（keys.onnx/keys.json、choice.onnx/choice.json，共 ~23 MB）：
#    没有它们 `analyze` 会**直接报错**（`auto` 不再静默退回启发式），
#    所以让仓库"clone 下来就能跑"值这 23 MB ✓
#  · **训练产物不入库**：models/runs/（66 MB 的 best.pt / 曲线 / 混淆矩阵）、
#    models/weights/（yolov8n.pt 预训练权重）、models/.ultralytics/（缓存）、
#    以及 keys_v1_uncorrected.onnx 这种旧备份
models/*
!models/README.md
!models/keys.onnx
!models/keys.json
!models/choice.onnx
!models/choice.json

# ---- 兜底：ultralytics 默认相对 CWD 建这两个目录 ----
# 代码里已把 runs_dir/datasets_dir/weights_dir 钉到 models/ 下（runner._pin_output_dirs），
# 但换个入口/换个版本它可能又冒出来 —— 别让它们进仓库。
runs/
Ultralytics/

# ---- 编辑器 / 系统 ----
.idea/
.vscode/
Thumbs.db
desktop.ini

# ---- 大文件：**不要**把跑图录像提交进来 ----
# 录像动辄几十 MB（一次分析还会产出上百张 PNG），所以 fixtures/ 只放
# 小体积的帧样本（PNG），视频用环境变量 A9ROUTE_VIDEO_DIR 指到外部目录。
*.mp4
*.mkv
*.avi
*.mov
*.webm
!fixtures/**/*.png
