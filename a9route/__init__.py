# -*- coding: utf-8 -*-
"""a9route —— 从跑图视频推断比赛操作，生成百分比路线脚本。

一句话：**把一段跑图录像喂进来，产出可以直接粘进路线脚本的扁平逗号流。**

## 为什么单独做一个项目

原来这套东西长在「手机自动化」那个项目里（`asphalt9auto`），
但视频分析这条线**一次都不需要手机**：只要录像文件 + OpenCV + OCR。
把它独立出来之后，调一个阈值（例如氮气的红占比）跑一次离线回归只要几秒，
不用再把整个手机自动化项目（adb / 标定 / 手柄 / Web 控制台）跑一遍。

## 两层判据（这是全部算法的骨架）

1. **粗扫**（`vision.scan`，默认每 0.25s 一帧）：
   逐帧算出「这一帧在做什么」+ 读到左上角「路程 NN%」，
   **每个整百分点存一张 PNG**，并把该百分点附近的判据汇总成一个操作。
2. **精细扫描**（`vision.scan_fine`）：逐帧（30fps）只看两个按键（刹车/氮气）
   + 每 2 帧看一次选路路标，**只产时间戳**；操作**目的**由信号形状判定
   （`core.intent`：成对脉冲=360、独立短脉冲=打断氮气、按住=漂移……），
   最后用「时间 -> 百分比」的插值关系把时间戳落回百分比。

## 实话实说

视频里**看不到按键真值**，所以判据都是**启发式**的，误报难免 ——
本项目的定位是「自动生成草稿 + 人工核对」，
`analyze()` 会把每个百分点的截图和判据一起摊开给你看，方便核对。
"""
from __future__ import annotations

import os

__version__ = "0.1.0"

if not os.environ.get("A9ROUTE_NO_CONSOLE_SETUP"):
    from a9route.bootstrap import configure_console as _configure_console

    _configure_console()
