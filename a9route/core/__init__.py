# -*- coding: utf-8 -*-
"""core —— 纯逻辑层（不碰图像库、不碰设备、不碰网络）。

* `route`  —— 路线脚本的格式：解析 / 校验 / 扁平逗号流。
  **格式与用户那份现成脚本 1:1 兼容**，是整个项目对外唯一的产品契约。
* `intent` —— 把"精细扫描"的按键信号按**形状**判成操作目的（360 / 漂移 /
  打断氮气 / 单击·双击·长按氮气 / 选路）。

这两块都是纯函数，离线测试覆盖得最狠（改格式或改规则先跑它们）。
"""
