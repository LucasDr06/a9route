# -*- coding: utf-8 -*-
"""vision —— 看画面：一帧进，判据/读数出。

| 文件 | 干什么 | 依赖 |
|---|---|---|
| `video.py` | 抽帧、时间↔百分比插值、逐百分点截图、精细按键扫描、段合并、生成路线 | OpenCV |
| `cues.py` | 一帧 -> "现在在做什么"（按键亮不亮、路标、状态文字） | OpenCV + OCR |
| `hud.py` | 一帧 -> 比赛读数（路程 NN% / 排名 / 选路路标） | OpenCV + OCR |

三块都**不连设备**：帧由调用方给（视频文件解出来的）。
所有阈值/框位置由 `a9route.config` 灌进来，见那个文件的说明。
"""
