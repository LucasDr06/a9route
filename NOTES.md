# NOTES —— 判据是怎么量出来的（以及哪些路走不通）

这份文件是**踩坑记录**，从原项目 `asphalt9auto/HANDOFF.md` §3.25~§3.34 里抽出来的、
**与视频分析有关**的部分。所有数字都是真录像/真截图实测的。

新会话改判据之前先看这里 —— 很多"想当然"的做法已经在下面被否掉了。

---

## 1. 一帧画面里"现在在做什么"：五路判据

`vision/cues.py`，都在真实录像 `output/跑图转路线测试.mp4` 上量过：

| 操作 | 判据 | 实测 |
|---|---|---|
| **氮气** | 顶部氮气槽左段**青色 vs 黄色**（喷氮=青） | 两者**互斥**：青 0.14~0.43 时黄 0.00，反之亦然 → 用"青占比 > 0.10" ✓ |
| **漂移** | 右侧状态区出现「**漂移NN米**」 | 比车尾烟雾/刹车图标稳 |
| **选路** | 上方**圆形路标**（HoughCircles） | 录像 27-28s 出 2 个、39-40s 出 3 个 ✓；蓝色高亮那个=当前选中 |
| **360** | 右侧「**完成360度旋转**」 | **游戏自己的提示**最硬（实测 9~19% 连做 360 时状态区 OCR = `腾空时间：2.77秒 / 完成360度旋转 / X3`） |
| **关自动驾驶** | 左上角「TOUCHDRIVE 关」 | 与实时流程同一个读数 |

后来（用户 2026-09-13 要求）改成**主要看按键亮不亮**，而不是看效果：

* `NITRO_KEY_BOX=(1033,497,110,110)`、`BRAKE_KEY_BOX=(137,497,110,110)`
  —— **整个圆**，圆心 (1080,552)/(200,552)、半径 ≈50，关于屏幕中线 x=640 完全对称；
* 氮气按下 = `红占比 > 0.15`；刹车按下 = `圈内白度 − 圈外参照白度 > 0.35`。

### ⚠️ 键/瓶子的语义要分清（实测澄清过两次）

* 右下**瓶子变红 = 氮气"已充满/可用"**，**不是"正在喷"**
  （12.8s/13.6s 红 0.98 而顶部槽青 0.00；两者只有 **65%** 一致）；
* 所以"正在喷"用**顶部氮气槽变青**、"玩家在按键"用**瓶子变红**，
  `Cue.describe()` 特意把两种文案分开：`氮气键按` vs `氮气槽青(在喷)`。
* 用户原话："**图标没点是透明的，按下变成部分白色不透明**"。

### 五种判据的实测对比（三条正样本）

正样本：标定视频刹车段 10~20s（用户标签）、氮气段 20~29s（用户标签）、
测试3 的 17%（用户报"这里点了两次氮气"）。表里的数是"窗口内均值 − 窗口外均值"：

| 判据 | 刹车段 | 氮气段 | 测试3@17% |
|---|---|---|---|
| 模板匹配（点亮模板，TM_CCOEFF_NORMED） | -0.106 ✗ | -0.171 ✗ | +0.039 |
| 模板差分（点亮−未点亮） | +0.012 ✗ | +0.021 ✗ | -0.083 ✗ |
| 亮白不透明（绝对白度占比） | +0.115 ✓ | +0.067 | -0.061 ✗ |
| 红（红占比） | +0.053 | **+0.150** ✓✓ | -0.012 |
| **圈内 − 圈外**（相对白度） | **+0.123** ✓✓ | +0.063 | -0.039 |

**结论**：

* **模板法最差，别再试** ✗ —— 图标是**半透明叠加**在场景上的，模板主要在匹配**背景**；
  "点亮模板 − 未点亮模板"差分后仍然不行（背景差异比图标本身还大）；
* **刹车**用"圈内白度 − 圈外参照白度"最准（阈值 0.53 时 按下 0.37 / 没按 0.11）；
* **氮气**用"红"最准（阈值 0.15 时 0.36 / 0.09）；圈内判据对氮气**没信号** ✗
  （测试3 按下 0.004），并进来只会把误报抬高。

---

## 2. 走过的死路（省时间，别重复）

* **整帧文字扫描**：`adaptiveThreshold` 在平坦区域整块变白（帧掩码 99% 白）、
  Chamfer 图全平、负样本满分 ✗；
* **Top-Hat + 向量化 Chamfer**：模板与整帧缩放比不一致，笔画粗细差一个量级 ✗；
* **用顶部那根进度条当路程**：三张截图里它在 22% / 34% / 37% 时长度不成比例，
  更像**氮气槽**（漂移时被点亮填满）→ **百分比只认左上角的数字读数** ✓；
* **选路用"亮块按列分组"**：真实画面上"一塌糊涂"——只有亮尘土没有路标的帧会误报 1~5 个 ✗；
  两个挨着真路标（间隔几像素）会被切成零件 ✗ → 换成 `cv2.HoughCircles` ✓；
* **`cv2.imwrite()` 遇到中文文件名会静默失败** ✗✗（不抛异常、只返回 False）：
  存图**一律用 ASCII 文件名**（所以截图叫 `001pct_t012.50s.png`，
  测试里专门有一条断言盯着这点）。

---

## 3. 两套时间基（真 bug，用户报"精细识别说 1%、百分比识别说 5%"）

* 粗扫原来是 `cap.set(POS_MSEC)` 之后按**请求的时间**记录 ✗
  （解码器常落在附近的帧上）；
* 精细扫描是顺序解码、时间是**真实 PTS** ✓ → 两条时间轴错位，事后填百分比必然偏 ✗；
* **修法**：`iter_frames()` 解码后改读 `cap.get(cv2.CAP_PROP_POS_MSEC)`，
  **一律报告真实解码时间**（提前解码线程那条路同样处理）。

> 改动细节在任何"帧时间"相关的代码里都要守住：**请求时间 ≠ 解码时间**。

---

## 4. 去抖必须分通道（用户报"17% 那两次氮气点击没识别到"）

* 那两帧的红占比正好 **0.151 > 0.15** —— **阈值本来就过得了** ✗；
* 真凶是 `scan_fine(hold=2)`"连续 2 帧才算按下"把只有 **1 帧（33ms）** 的点击整段抹掉了 ✗✗；
* **修法**：`scan_fine(hold=1, hold_brake=2)` ——
  氮气（点按）允许单帧；刹车（长按）要求连续 2 帧。
* 修完测试3 出现 `17,N:0:2:67` ✓（正是用户报的那两次点击，实测间隔 67ms）。

---

## 5. 选路（岔路口）：数量、最后一次、稳定性

* 用户规则："一屏只有 **2/3/4** 条路"、"**蓝色高亮**的那个 = 当前选中的路"；
* **至少要 2 个路标**才算岔路口（单个圆多半是背景物）；
* **至少两帧稳定出现**才算 —— 不加这条时误报 42 次 ✗；
* **取最后一次**：玩家进岔路口时默认高亮第 1 条，但可能在图标显示期间**改点**另一条，
  只有最后一次高亮的才是真正走的路 → 原来取"图标最多的那一帧"（等效于第一帧 ✗），
  实测同一条录像从 `4,32` 变成 **`4,31`**（玩家最后改选的那条）✓；
* **HoughCircles 偶发多认一个圆**（用户 2026-09-13 报"实际只有 2 个却判成 3 选 3"）→ 两层修：
  `choice_icons()` 加"像一排路标"过滤（同高 ±22px + 半径中位数 0.6~1.7 倍，超过 4 个只留最靠中位高度的）；
  选路加**稳定性闸**（同一个值连续 4 帧才认）。测试3 的选路条目从 16 条降到 6 条 ✓；
* **交叉校验**：精细扫描说是岔路口，**粗扫也必须在 ±1 个百分点内看到 ≥2 个路标**才算
  —— 两边都说是，误报几乎清零 ✓。

> 已知还剩一条不准：用户说真实 5 个岔路口，输出 6 条
> （`4,21 / 5,22 / 21,43 / 27,22 / 74,31 / 77,32`），
> 很可能是开头 `(4,21)+(5,22)` 其实是同一个被读成两个值。
> 建议把"1 个百分点内的改变"合并成一条（取最后一次），或把稳定性闸提到 6 帧。

---

## 6. 按键段：点松被并成长按（真 bug）

`_episodes()` 原来拿"**最后一帧松开**的时刻"算间隔（`t - last_off`），
那永远等于"上一帧"（≈33ms），**永远不会超过 gap**，
于是用户 0.5s 一次的点松被并成一个 8 秒长按 ✗✗。

```python
# 错的：t - last_off       （永远 ≈ 一帧）
# 对的：off_len = 本次按下帧时刻 - 第一帧松开时刻
```

* 用户标定视频的波形：按下 0.64~0.66、松开 0.00，中间 5~7 帧过渡 —— 干净的二值信号，
  `gap=0.12s` 正好切开 ✓；
* 修完后合成点松（0.5s 按 + 0.3s 松 ×5 秒）→ **7 段**、用户标定视频 → **18 个按键动作**
  （11 漂移 + 7 氮气：`D:384~D:1051`、`N:100:1:100`×5、`N:0:2:100`×2）✓。

---

## 7. 360 与漂移是同一个键，会互相吃

* 360 = **双击刹车键**（用户澄清：不是划动手势 ✗）；
* 漂移 = **长按同一个键** —— 长按会把双击吃掉 ✗；
* 三层都挡：生成路线时（同一百分点有 360 就不写漂移）、
  运行时（`RouteRunner` 的漂移时间窗里有 360 就不发这条漂移）、测试 T9。
* 360 的判据阈值从 200ms **放宽到 300ms**（`tap_360_gap`）——
  200ms 太紧会把稍慢的双击判成"独立脉冲（打断氮气）"✗。

---

## 8. 输出格式（用户指定的两条）

1. **扁平逗号流**：`1,31,2,N:0:2:750,4,360,…` 一整行，不是"一行一条" ✗。
   Web 的「复制路线」只复制那一行。
2. **同一段里多个操作用 `|` 连接**（= 同时执行），例如 `9,D:850|360`、`40,21|D:2050`。

另外两条实测调出来的处理：

* **连续同类合并**（`collapse_runs`）：判据是逐帧的，不合并会写出 75 条碎条目
  （一个岔路口横跨 4 个百分点就写 4 条选路 ✗）→ 合并后 **18 条** ✓；
* **"用没用上按键细扫"必须写在文件头部**：用户就因为看错这个，
  把"窗口汇总"估出来的整数时长（`D:4000`/`N:0:12:100`）当成真值 ✗。

---

## 9. 跨平台/工具类坑（都真实踩过）

* **Windows 中文控制台是 GBK**：打印 `✓` 直接崩
  （`UnicodeEncodeError: 'gbk' codec can't encode character '\u2713'`）
  → `bootstrap.configure_console()` 统一 reconfigure + 切代码页 65001
  （`a9route/__init__.py` 导入时自动调用，`A9ROUTE_NO_CONSOLE_SETUP=1` 可关）；
* **PaddleOCR 初始化会刷屏**（connectivity check / Creating model / ccache warning /
  子进程 `where` 的结果）→ 必须 **import 和创建都包进静音上下文**，
  且要 dup 进程级 fd 1/2（子进程那行 Python 层拦不住）；
* **OCR 缓存默认在 `~/.paddlex`**：项目里若有 `worktmp/paddlex` 就优先用它
  —— 受限沙箱里读不到工作区外的模型（实测报
  `PermissionError: ...\.paddlex\official_models\...\inference.yml`，
  看着像"OCR 没装"，其实是被沙箱挡住的）；
* **路径不要相对 CWD**：原来 `Path("config")` 散在 4 个文件里，换目录启动就读错
  → 统一收口到 `paths.py`；
* **产物会涨到 GB 级**（每次分析上百张 PNG）→ `worktmp/` 可以随时整个删掉，
  产物目录按视频名分开，方便单独清理。

---

## 10. 按键识别改成 YOLOv8（2026-09-14 加的训练框架）

框架在 `a9route/train/`，用法和"要准备什么"见 `TRAINING.md`。这一节只记**踩到的坑**。

### 10.1 ⚠️ 项目里其实有**两套**启发式，而且默认那套可能有问题

这是接线时发现的，**没有改行为，只是把它摊开测了**：

| | 刹车 | 氮气 |
|---|---|---|
| `scan_fine()` —— **产出路线里 `N:`/`D:` 的就是它** | `bright_ratio > 0.35`（**只看绝对白度**） | `max(red, ring) > 0.15`（**红 ∪ 圈内−圈外**） |
| `cues.key_pressed()` —— 粗扫（`Cue`）在用 | `ring > 0.35 or white > 0.35`（**并集**） | `red > 0.15`（**只认红**） |

两边都不一致；更要紧的是 `scan_fine` 的氮气把**圈内−圈外**也并了进来，
而 NOTES §1 的实测结论是：

> 圈内判据对氮气**没信号**（测试3 按下 **0.004**），并进来只会把误报抬高 ✗

`cues.py` 里 `key_pressed()` 的注释也写着"**氮气只认红 >0.15**"。
所以 README「氮气偏多」那条已知问题，**至少有一部分是"把没信号的通道并进来了"**，
不是"阈值调得不够好"。

* 默认口径**保持现状**（`vision.key_heuristic_mode="fine"`，逐字不变）；
* 想验证就 `--set vision__key_heuristic_mode=cues`，再用
  `a9route train pulses <录像>` 对比两边的动作数（它会并排打、带"差"这一列）；
* `test_train.py` 的 T10 用一帧"氮气框整块白、不含红"的图像把这两个口径的
  差异**钉死在测试里**（fine 判按下、cues 判没按）。

### 10.2 抽帧不能"翻转帧无限抽"（真数据打脸）

第一版按优先级抽帧，让最重要的 `edge`（翻转帧）不设上限。三段真录像上实测：

```
1146 帧里抽出 448 帧，全是 edge      ← 预算 400 被冲爆
三段合计 1341 帧里 edge 1253，pos/bg 一个都没有
```

原因：**启发式在阈值附近会抖**，一次按下产生好几个"翻转"，
每个翻转 ±3 帧的窗口连起来就盖住整段视频，`reason` 退化成"什么都是 edge"。

修法两层：
1. `edge` 最多占 `edge_share`（默认 60%），超了**沿时间均匀取样**（保住整段覆盖）；
2. 还得给 `bg` **预留** `bg_share`（默认 20%）—— 光限制 edge 不够，
   `pos` 会把剩下的全吃掉（实测：抖动信号 + 预算 40 → edge 24 + pos 16 + **bg 0**）。

修完实测（三段录像、预算 1200）：

```
每段 400 帧：edge 240 / disagree 80 / bg 80
合计 1200 帧 / 175 MB（约 145 KB/帧）
框：刹车 291、氮气 193；纯背景帧 727；双键同时按下 11
```

### 10.3 2 类检测 + ONNX = 一个**静默**的坑

YOLOv8 导出的 ONNX 有两种：

* 未做 NMS（`yolo export` 默认）：`(1, 4+nc, N)`
* 已做 NMS（`nms=True`）：`(1, N, 6)` —— `[x1,y1,x2,y2,conf,cls]`

我们**只有 2 类** → `4+nc == 6` → **两种形态的行宽一模一样，光看形状分不出来**。
猜错**不报错**，只会把坐标当分数用（置信度会被读成类分数、框全乱），
看起来像"模型训废了"。

处理：`vision.key_fmt`（`auto`/`raw`/`nms`），`auto` 按"未做 NMS"解析 ——
因为 `train export` **不传 `nms=True`**。你要是自己用 `nms=True` 导过，
**必须**设成 `nms`。`test_train.py` T9 把这个歧义钉住了
（同一段 6 宽数组，auto 读出 conf=1.0、`fmt=nms` 读出真值 0.90）。

### 10.4 跨环境：训练要 torch，运行只要 onnxruntime

本机两个环境：

* `ai_joy`（Python 3.9.25）有 **torch 2.8.0+cu128** —— 正好支持 RTX 5070 Ti
  的 **sm_120**（`cap (12, 0)`，实测 CUDA 可用）。装 `ultralytics` 后就能训。
* `alphash9auto`（Python 3.12）是 `a9route` 的运行环境，只有 cv2/numpy。

所以**导出 ONNX、运行时用 onnxruntime（约 15 MB）**，不必为了推理在运行环境里
塞 2.5 GB 的 torch。`train doctor` 会把"当前环境能干什么、缺什么、怎么补"一次说清。

### 10.5 这台机器上"下载"这件事有一半是坏的

* **`curl` / `Invoke-WebRequest` 的 schannel 挂了**
  （`schannel: AcquireCredentialsHandle failed: SEC_E_NO_CREDENTIALS`）；
  而 **ultralytics 下载权重就是 shell 调 curl** →
  `YOLO("yolov8n.pt")` 报 `Download failure ... Curl return value 35` ✗✗；
* **Python 自己的 TLS 是好的**（`ssl`/urllib，实测 pypi/github/modelscope 都通，
  github 走本地代理约 9s）→ 所以 `a9route train fetch` 用 **urllib** 下权重；
* `pip` 也要走镜像（`-i https://pypi.tuna.tsinghua.edu.cn/simple`），
  直连 pypi 会卡死；
* `pip install` 会往 conda 环境的 site-packages 和系统临时目录写 ——
  在受限沙箱里需要放宽权限。

### 10.6 受限环境里跑训练要绕三道弯（**都不是代码问题**）

| 卡在哪 | 根因 | 绕过 |
|---|---|---|
| `mkdir ~/.matplotlib` / `~/.config/Ultralytics` 失败 | HOME 不可写 | **已在代码里修**：`runner.prepare_env()` 把 `MPLCONFIGDIR` + `YOLO_CONFIG_DIR` 都指到项目内（和 §9 里 `.paddlex` 同一个解法） |
| `PermissionError`，栈底 `multiprocessing → connection.Pipe → _winapi.CreateFile` | ultralytics 建标注缓存时会开 `Pool`，Windows 上 `Pool` 靠**命名管道** | 放开该次命令权限，或预先让缓存存在 |
| `DataLoader worker (pid(s) …) exited unexpectedly` | 同一个根因：DataLoader worker 之间**也走命名管道** | **`--workers 0`** |

> 普通终端里跑训练**不会**遇到这三条；只有在这种受限沙箱里才要绕。
> 写进 TRAINING.md §7 免得下次又查半天。

### 10.7 评估**别看 mAP**

两个按键位置固定、框永远一样大 → mAP 天生接近满分，什么也证明不了。
`train/runner.evaluate_frames()` 直接算**逐帧**的 P/R/F1，并且：

* 把**启发式当对照组**并排打出来（模型没赢就明说"别切后端"）；
* 打 **`fp_per_1k_bg`（每 1000 张背景帧的误报数）** —— **"氮气偏多"就是这个数**；
* 打**翻转帧正确率**（单击/双击边界那批最难帧）。

另外 `train pulses <录像>` 给整片的**动作数对比**（模型 vs 启发式，带"差"这一列）——
在 `刹车氮气标定测试.mp4`（用户亲手标定过刹车 10~20s、氮气 20~29s 的那段）上跑，
是**唯一不靠启发式也能判断好坏**的检查。实测：刹车 21 个动作（启发式 18）、
**氮气 17 个（启发式 23）** —— 方向正是"把偏多的氮气收回来"。

### 10.8 ⚠️ 用一个假基准"证明"模型赢了（真踩，而且很隐蔽）

第一次跑 `train eval` 得到的是这个（**错的**）：

```
对照：启发式 cues  brake F1 0.789（召回只有 0.652）
模型               brake F1 0.909        ← 看着像"模型大胜"
```

但实测 `HeuristicKeys("cues")` 在 val 上和标注**逐帧完全一致**（400/400）。
差 35% 召回说明**评估时的启发式和打标注时的启发式不是一套**。

根因：`cues.BRIGHT_THR` 的**模块默认是 165**，而 `DEFAULTS` 里是 **150**
（`apply()` 灌进去的是 150）。`dataset.build` 调了 `apply()`，所以标注是按 150 打的；
而 `cmd_train` 当初**只在带 `--set` 时才调 `apply()`**，于是 `train eval` 拿 165 去复算 ✗。

修法：`cmd_train` 无条件 `cfgmod.apply()`。

**教训**：`config.py` 的模块文档早就写着"任何入口（CLI / Web / 测试）都必须先调一次
`apply()`"，漏一次就会得到一个**看起来很合理但完全错误**的结论 ——
而这次它恰好"证明"了我想看到的结果（模型更好），所以特别危险。

### 10.9 ⚠️ 拿启发式打的标注当"答案"，评不出任何东西

修完 10.8 之后数字变成：

```
对照：启发式 cues  F1 1.000 / 1.000   ← 必然满分：标注就是它打的
模型               brake 0.909 / nitro 0.876
```

"模型没有比启发式更好"这句话**技术上对、但毫无信息量** ——
拿自己打的标注考自己，当然满分。

所以 `evaluate_frames()` 现在把**复核进度**一起带进报告
（`reviewed_frames` / `labels_are_truth`），`format_frame_eval()` 在标注没复核过时
**明说**"这个对比没有信息量"，并告诉用户先 `train review` + `train apply`。
`test_train.py` T11 把这个行为钉住了（没复核时**不许**打「模型更好」这类结论）。

对照表也改成**两行**：`cues`（和标注同口径的公平对照）+ `fine`（**线上真正在跑的**）。
这个区分是有意义的 —— 见 10.1：`fine` 的氮气误报实测 **223.1/千帧背景**，
而模型在同一批帧上是 **12.4**。

### 10.10 顺手修掉的一个既有 bug：`s.buttons` 恒为空

`PercentShot.buttons` 只有**旧的** `vision.attach_events()` 会填；
现在的主线（`extract_per_percent`）填的是 `s.intents`。于是 `analysis.analyze` 里

```python
n_btn = sum(len(getattr(s, "buttons", []) or []) for s in shots)
if with_buttons and n_btn == 0:
    rep.warnings.append("按键细扫没有结果（阈值可能不合适，或视频里没有按键动作）")
```

**恒成立** —— 哪怕精细扫描明明判出了 28 个操作，也会打这句警告 ✗✗。

**这正是用户最怕的一类错误**（README/NOTES 记着：他就因为看错"用没用上按键细扫"，
把窗口汇总估出来的整数时长当成了真值）。修法：`n_btn == 0 and n_it == 0`
才算真的没有结果（`n_it` 是 `s.intents` 的条数）。
**判据一行没动，改的只是那句会误导人的警告。**

### 10.11 标注是"按视频帧算的"，但训练读的是"存下来的 JPEG"

`build` 原来是：解码视频帧 → 算判据 → **存成 JPEG** → 写标注。
于是标注描述的是**视频帧**，而训练读到的是**有损压缩后的 JPEG** —— 两者在有阈值的
判据上不是同一个东西。

实测抓到一帧（`3_001402.jpg`）：

```
视频帧上   brake_white=0.366  (> 0.35 判「按下」)  余量 1.046
JPEG 再读  brake_white=0.338  (< 0.35 判「没按」)  余量 0.966
```

那一帧离判定线只有 **4.6%**，q92 压缩就把它推过去了。**1200 帧里出现 1 帧（0.08%）**。

影响很小，但它是"数据集体检永远报 1 帧漂移"的来源，而且模型在学一个和它看到的
像素不一致的标签。**修法：`build` 存完 JPEG 之后，用那张 JPEG 重新算一遍判据再写标注**
（`dataset._RELABEL_FROM_JPEG`），并把这种帧标成 `jpeg_flip`。
修完重建数据集，体检从"1 帧漂移"变成干净的 0（`brake_pressed` 框 291 → 290，正是那一帧）。

> 顺带说明：**评估永远不可能达到 F1 = 1.000** 也有这个因素在里面 ——
> 贴着阈值的帧本来就在噪声里，这不是 bug。

### 10.12 同一帧有**两份**标注，改错那一份等于没改

`split` 是把 `pool/` 拷进 `images/{train,val}` + `labels/{train,val}`。
于是同一帧有两份标注，而**体检/训练读的是 `labels/{train,val}/` 那份**。

用户按文档去手改 `pool/labels/*.txt` → **完全不生效**，而且没有任何提示 ✗✗。
（写文档的时候我自己就是这么写的："想直接手改也行：标注文件就在
`pool/labels/*.txt`" —— 那句话是错的，已改。）

修法：`audit()` 加一条检查，**逐帧比对 pool 和 train/val 两份标注**，
不一致就报 error，并说清"训练读的是 train/val 那份"。这条检查在写它的时候
立刻抓到了我自己测试里的一个漏还原（T8 只还原了 pool 那份）。

### 10.13 图形界面：数据集体检页（`/dataset`）

命令行 `a9route train audit` 之外，`a9route serve` 多了**第二个页面**
（`web/dataset.py` 是个 Flask Blueprint，业务逻辑仍在 `train/audit.py`，两边同一份）：

* **体检表**：布局对应、**标注框 vs 当前标定框**、标定自身对称性、分辨率、
  **pool↔train/val 两份是否一致**、**用当前判据重算 vs 存下来的信号/标注**；
* **逐帧对照**：缩略图上画**实线=标注框**、**虚线=当前标定框**（应当完全重合），
  右边两张是按键框的放大图；可按 划分/抽样原因/类别/复核状态 过滤，能"只看有问题的帧"。

两条设计取舍，都是为了**不让人误判**：

1. **框用百分比定位**（`x/imgW*100%`）画在 `<img>` 上，而不是服务端烤进图里 ——
   这样缩放多少都对得上，而且开关标定框不用重新请求。
2. **放大图在服务端裁**（`audit.crop_preview`）—— 前端用 CSS 摆缩放/偏移很容易
   差几个像素，而差几个像素就看不出"透明 vs 半透明白不透明"的区别，
   那正是判据要看的。服务端裁能和判据读**同一块像素**。

`test_train.py` T13 里有 **5 个"故意弄坏"的反例**（改 config.json 的标定框、
改阈值、删标注文件、把框写歪、只改 pool 不重划分），每条都要求体检**必须抓住** ——
**一个永远说 OK 的体检是没有价值的**。其中"改标定"那条必须**走 config.json**，
不能直接改 `cues.BRAKE_KEY_BOX`：`audit()` 开头就 `cfgmod.apply()`，
会把猴子补丁覆盖掉（第一版就是这么"测试通过"的，其实什么都没测到 ✗）。

### 10.14 用户一复核，页面就变红（把"人工修正"当成了"漂移"）✗✗

用户按页复核了 30 帧（把启发式误报的"刹车按下"改成"没按"），体检立刻报：

```
[error] 抽检 200 帧：判据已经和标注不一致     漂移 {'label_drift': 5}
```

可那 30 帧**正是人干的活**。根因：体检比的是"标注 vs 当前判据重算"，
却**没区分这一帧有没有人复核过**：

* `status == "reviewed"` -> **人工修正**（预期，而且正是我们要的增量）；
* `status == "prelabel"`  -> **真漂移**（阈值/框被改过）。

不分的后果很严重：**人越认真复核、页面越红，最后就学会无视红色了**。
实测分开之后：全量 1200 帧里 `reviewed 且不同` = 30、`prelabel 且不同` = **0**。

修法：`audit()` 把两者分开统计（`human_fixed` vs `label_drift`），
逐帧页给人工修正的帧打**蓝色** `human_fix` 提示（不是橙/红）；
`only="problem"` 靠一个新的 `PROBLEM_FLAGS` 白名单**把 `human_fix` 排除在外**
（否则"只看有问题的"会刷满用户自己的修正记录 —— 这条是被 web 探针当场抓出来的）。

> 教训：**"人做的"和"机器漂移"在数据上长得一模一样，必须靠额外的状态字段区分**，
> 否则会把好事报成坏事。

### 10.15 "翻页即复核"：批量动作**绝不能**复用写标注那条路

用户的复核习惯是按页翻：**有错的才改，没错的直接翻过去**。让他为每张正确的帧
点一次「没问题」是纯浪费，所以界面在**离开本页时**自动结算。

关键设计：**两个动作必须走两个接口**。

| | 接口 | 会不会写标注文件 |
|---|---|---|
| 人改过的帧 | `/api/dataset/corrections` | **会**（按当前标定对齐框） |
| 没改过的帧 | `/api/dataset/mark_reviewed` | **不会**，只改 `meta.jsonl` 的 `status` |

为什么不能统一用 `corrections`：它写框时用的是**当前 `cues.BRAKE_KEY_BOX`**。
数据集要是用**另一套标定**建的，翻一页就会把整页标注**悄悄挪到新位置**，
顺手把"数据集和标定对不上"这个体检结论也抹平了 —— 而那正是体检要报的错 ✗✗。

对应两条硬断言（`test_train.py`）：标注文件**逐字节比对没变**；
`changed == 0` 时**不覆盖** `last_marks`（免得"重翻一次同一页"把撤销点冲掉 ——
这条也是测试当场抓出来的）。

还有一条：**框和标定对不上的帧不会被自动记成已复核** —— 不能让"翻过去"这个动作
把问题藏起来。工具栏另有「撤销上次标记」（只认最近一次 = 上一页的粒度）。

### 10.16 用户复核 572 帧之后：**两个通道的病根完全不同**（本轮最值钱的实测）

用户按页复核了 572 帧（train 466 / val 106，其中约 115 帧真的改了标注）。
拿**人工答案**当基准（这是唯一可信的基准，见 10.9），三个人工复核的 val 子集
（106 帧）上的成绩：

| 刹车按下 | 精确率 | 召回 | F1 |
|---|---|---|---|
| 启发式 cues（预标注） | 0.111 | 1.000 | 0.200 |
| 旧模型（未修正标注训） | 0.123 | 1.000 | 0.219 |
| **新模型（修正标注训）** | **0.875** | **1.000** | **0.933** |

| 氮气按下 | 精确率 | 召回 | F1 |
|---|---|---|---|
| 启发式 cues | 0.108 | 1.000 | 0.195 |
| 旧模型 | 0.148 | 1.000 | 0.258 |
| **新模型** | **1.000** | **1.000** | **1.000** |

#### 刹车：病根是「绝对白度」那一路（**一行配置就能修**）

`key_pressed()` 对刹车是 `ring > 0.35 or white > 0.35`（两个通道取并集）。
在人标"未按"、启发式却判"按下"的 **118 帧**里：

* **117 帧（99%）是"只有绝对白度越线"** —— 圈内−圈外 平均 **0.049**（阈值 0.35），
  绝对白度 平均 **0.534**；
* 而人标"按下"的 **102 帧，圈内−圈外全部越线（102/102）**。

也就是说：**「圈内−圈外」这一路自己就够用，而「绝对白度」这一路几乎只贡献误报。**
这和 NOTES §1 的原则（"半透明图标必须用相对量，绝对值会被赛道明暗带走"）完全一致 ——
`white` 那条 OR 分支恰恰违背了它。

实测**一行配置就能修**（不用模型）：

```powershell
& $py -m a9route train eval --weights models\keys.onnx --set vision__brake_white_thr=1.0
# 启发式 cues 的刹车 F1：0.200 -> 0.933（和模型打平）
```

#### 氮气：**调参救不了**，红占比和"按下"是**反相关**的

同样的分析做在氮气上，结果完全相反：

```
人标「没点氮气」但启发式判「按下」: 108 帧   红占比 中位 0.466
人标「氮气按下」:                  32 帧   红占比 中位 0.246
```

**误报的红占比比真按下的还高**。逐阈值试：

```
阈值 0.15: 误报剩 108/108   真按下剩 28/32
阈值 0.25: 误报剩  88/108   真按下剩 15/32
阈值 0.35: 误报剩  70/108   真按下剩  4/32
阈值 0.50: 误报剩  51/108   真按下剩  0/32     <- 真按下先死光，误报还剩一半
```

这正是 NOTES §1 记的那个语义坑：**瓶子变红 = 氮气"已充满/可用"，不是"正在点"**。
两个状态在"红占比"这一个量上不但重叠、而且是**反向**的 —— 任何阈值都不可能分开。
**所以氮气才是模型真正不可替代的地方**（同一批帧上模型 F1 = 1.000）。

> 结论：**先能用配置修的，就别用模型**；模型留给"判据本身分不开"的那部分。
> 刹车是前者（`brake_white_thr=1.0`），氮气是后者。

#### 顺带：训练指标也跟着变好了

| | 旧（未修正标注） | 新（修正标注） |
|---|---|---|
| mAP50-95 | 0.935（epoch 39） | **0.970**（epoch 21，提前停） |
| 翻转帧正确率（val） | 0.926 | **0.982** |
| 整片刹车动作数（标定视频） | 21 | **11**（启发式 18） |

旧模型在"标定视频"上报 21 个刹车动作，是因为它忠实学会了"绝对白度那一路"的误报。

### 10.17 ✗✗ 「识别不到任何操作」的真凶是 **OCR 读不到模型**（不是检测）

用户报："现在识别路线识别不到任何操作"。

复现出来的现场：

```
扫完 153 帧，得到 0 张百分比截图      <- 粗扫一张图都没存下
路线正文（0 条操作）
```

**跟检测毫无关系。** 逐层查下去：

| 层 | 现象 |
|---|---|
| 粗扫 | 解码正常（153 帧都读到了） |
| `RaceReader.percent` | **恒为 `None`** |
| `OcrReader.last_error` | `PermissionError: ... 'C:\Users\Admin\.paddlex\official_models\PP-OCRv5_mobile_det\inference.yml'` |
| 根因 | `~/.paddlex` 在**工作区外**，受限环境读不到 -> OCR 起不来 -> 读不到「路程 NN%」-> 每个百分点都进不了 `seen` -> **路线必然是空的** |

`notes` §9 早就记过这个坑（"项目里若有 `worktmp/paddlex` 就优先用它"），
但**没有任何东西去创建那个目录** —— 而且 `reader.py` 里还硬编码了
`ROOT/"worktmp"`，没走 `paths.WORKTMP_DIR`。

#### 修法（两层）

1. **自动镜像**（`ocr.reader.ensure_cache()`）：发现默认缓存"读不到"就把
   **真正要用的两个模型**（`PP-OCRv5_mobile_det` 4.7 MB + `rec` 16.4 MB，
   共 21 MB；默认缓存 240 MB 里另外那些是 server 版/方向分类/UVDoc，本模块已关掉）
   抄进 `WORKTMP/paddlex`，再把 `PADDLE_PDX_CACHE_HOME` 指过去。
   另外给了 `a9route ocr status|cache`：`cache` 会**强制**抄一份
   （在普通终端里跑一次，之后受限环境也能用）。
2. **响亮报错**：`AnalysisReport.blocker` —— 一张图都没扫到时，
   `analyze()` 会给出**具体原因 + 能直接照做的命令**，CLI 和 Web 都把它当
   **错误**显示（界面弹红框、pill 变"无效"）。
   在这之前只有两句温和的 warning，看起来就像"这视频里没有操作" ✗✗。

#### 两个连带踩到的 Python/沙箱细节

* **`Path.is_file()` 会抛 `PermissionError`**（`WinError 5`），不是"返回 False"。
  第一版的状态探测没包 `try`，于是"检查默认缓存"这步直接炸，
  **根本没走到镜像那一步** ✗。所以 `_probe()` 只回答
  `"ok" / "missing" / "denied"`，**永远不抛**。
* **"没有"和"读不到"必须分开**：前者要联网下载，后者只需镜像。
  第一版没分，于是受限环境被误判成"找不到模型"，给的提示完全是错的。

### 10.18 默认后端改成 `auto` 之后，**测试就不再是"同一份代码同一结果"了**

把 `vision.key_backend` 默认改成 `auto`（`models/keys.onnx` 在就用模型）之后，
`test_video` 端到端那条立刻红了：

```
[FAIL] 逐帧认出了按住刹车的那些帧  — 0
[FAIL] 逐帧认出了氮气按下的帧      — 0
```

原因很有意思：**它悄悄换成了真模型**，而合成帧是"画出来的白圆/红圆"，
真模型（在真实游戏画面上训的）根本不认 —— 认不出是对的 ✓。
但这也意味着：**同一条测试在"没训过模型"的机器上过、在"训过模型"的机器上挂** ✗✗。

修法（`tests/__init__.py`，两道闸一起上）：

1. `paths.CONFIG_FILE` 指到**不存在的文件** —— 测试读的是**代码内置默认值**，
   而不是"这台机器上恰好被改成的样子"。
   只钉 `key_backend` 是不够的：`scan_fine` 会重新 `load_config()`，
   而 `config.json` 里正好写着 `onnx`（`train use` 写的）✗；
2. `paths.DEFAULT_KEY_MODEL` 指到不存在的文件 —— 即使后端是 `auto`，
   也只会退回启发式。

同时**补了一条真正覆盖模型接线的测试**（不依赖任何模型文件）：
往 `scan_fine(keys=...)` 注入"永远按下"和"永远没按"两个假后端，
断言输出跟着变 —— 证明"换后端"这条线是通的。
（这就是上面那个 0 暴露出来的**覆盖盲区**：接线没被测过。）

> 教训：**默认值一旦依赖"磁盘上有没有某个文件"，测试就不再是可复现的。**
> 要么把测试环境钉死，要么给覆盖盲区补一条显式注入的测试。

---

### 10.19 选路也改用模型：**三个选择题** → YOLO 标签

需求是"标定时给三个选择题：① 有没有选路 ② 有几个选项 ③ 选第几个"。
这三题**不能**当成三份独立数据存 —— 那就又多了一份会和标注打架的真相。
所以落成**一套折叠规则**，答案和标注文件互为唯一真相：

| 答案 | 标注 |
|---|---|
| 没有（①=否） | **空文件**（YOLO 的"背景帧"） |
| 有 N 个，选第 k | N 个框，按中心 x 排序，第 k 个是 `choice_selected`，其余 `choice_icon` |

* `boxes_from_answers()` / `answers_from_boxes()` 是一对互逆函数，界面显示的第 ③ 题
  是**从标注文件反推**出来的 —— 不存在"答案存了 3、标注只画了 2 个"这种撕裂；
* ② 只给 **2/3/4**：1 个图标是普通路标，不是岔路口（这是用户口径，写死在
  `MIN_OPTIONS=2`）；
* **类别是另外一套**：`choice_icon` / `choice_selected`，和 `brake_pressed` /
  `nitro_pressed` 不共用。清单里的 `classes` 让 `train use` 和运行时都能**认出
  "这是哪个任务的模型"**，切错了直接报错（`modelcard.identify()`）；
* 数据集**硬链接复用**按键那批帧（1200 帧实测 link=1200 / copy=0，额外 0 字节），
  并**照抄源数据集的 train/val 划分** —— 自己重分会让同一段视频出现在两边，
  评出来的分数是假的（10.9 那条教训的翻版）。

> 为什么另开一个页面（`/choice`）而不是在 `/dataset` 上加：
> 两个任务的**判据完全不同**（按键是"两个固定框里亮没亮"，选路是"一排图标里选了哪个"），
> 挤在一页会把按钮/快捷键/预览图全搅在一起。分开的代价只是多一个 blueprint，
> 但接口、页签、快捷键各自独立，改其中一个不会碰坏另一个。

### 10.20 ⚠️ `_decode` 从「每类一个置信度」改成「每条检测带坐标」

按键只需要"刹车亮没亮"，所以最早 `_decode` 返回 `{类名: 置信度}`。
选路不行 —— 要把**选中的那个图标框**画回画面上给人看，
于是改成 `[(类号, 置信度, (x1,y1,x2,y2)), ...]`（原图坐标，已逆 letterbox）。
顺带暴露/修掉两件事：

1. **`fmt="nms"` 那一支不再自己数 NMS。** 导出方说"已经做过 NMS"就相信它，
   原样照收（否则同一条框会被自己再滤一次，边界上会莫名少框）。
   自己 NMS 只发生在**未做 NMS** 的 `(1, 4+nc, N)` 形态上 —— 测试里两条对照着钉住了；
2. **旧断言有个洞**：用 dict 比"两类都解出来了"，**同类的 N 个框只会剩最后一个值**，
   "框的个数"这件事根本没被测到。新断言同时查个数和坐标，
   于是 `(1,4+nc,N)` / `(1,N,4+nc)` / `(1,N,6)` 三种形态 + NMS 行为 + 逆 letterbox
   全都在测试里了。

> 教训：**读输出的函数返回什么形状，决定了调用方"能看见什么"。**
> 把坐标丢掉的那一刻，"选中的是哪一个"就再也拿不回来了。

### 10.21 界面上的坐标**必须问后端要**，不能在前端自己算一遍

选路标定页要在"选路带"那张放大的图上点框，再把坐标换算回整帧。
前端一开始自己算：`裁剪矩形 = 常量(CROP_W, CROP_H) + pad`。
但后端裁图时**另外加了 10px 留白**（`BAND_PAD`），前端不知道 ——
于是**所有点上去的框整体偏了 4.2%**（画面越大偏得越多，最多偏 450px），
而且**不报错**：点出来的框看着"差不多在图标旁边"，人眼很难判定是偏了。

修法是接口把裁剪矩形一起吐出来（`crop_rect`），前端**只做平移缩放**，
不重复推导裁剪规则。实测往返误差 0（旧算法最多偏 450px）。

> 教训：**同一条几何规则在两处各写一遍，迟早会漂。**
> 谁裁的图，谁就要把坐标原点和缩放比例一起告诉对方。

### 10.22 ⚠️ 「改动 N 帧」把**没动过的帧**也算成改动了（撤不掉 + 假消息）

选路页的写回接口一开始是这么计数的：

```python
changed += 1        # 每处理一条就 +1
```

于是两个后果，一真一假：

* **假消息**：界面横幅写的是「提交 24 帧（**改动 24 帧**）」——
  人翻了一页什么都没点，也被告知"改了 24 帧"；
* **真的丢东西**：`last_answers`（撤销点）**只在 `changed` 非 0 时**才写，
  而它是**整体覆盖**的。所以"翻页顺手提交一整页没动过的帧"会把撤销点
  顶到这次空提交上 —— 人再点「撤销」，回到的是**空提交那一刻**，
  而不是他上一次真正的改动 ✗✗。

这个坑按键那边已经踩过一次（§10.15：`mark_reviewed` 的无操作覆盖了 `last_marks`），
**同一个形状的 bug 在另一条线上又长了一遍** —— 因为计数语义是各写各的。

修法：把"处理了"和"改了"分开，并且**内容真的不同才落盘**
（`labels.write_label_if_changed()`，顺便省掉无意义的 mtime 变动）：

```python
p, wrote = write_label_if_changed(p, boxes, w, h)
real = (wrote or m["answers"] != old_ans or m["status"] != old_status
        or m["manual"] != old_manual or m["position_guessed"] != old_guess)
changed += 1 if real else 0
same    += 0 if real else 1
```

返回值因此多了 `same` / `processed`，界面改成
「提交 24 帧（改动 3 帧，21 帧本来就对）」—— **只报真的发生了的事**。

> ⚠️ 注意"只有复核状态变了（`prelabel` -> `reviewed`）**也算改动**"：
> 翻页即复核这件事本身就是用户给出的信息，不能当成空操作。
> 测试里两种都钉住了：内容全同 -> `changed=0` 且**撤销点不动**；
> 答案变了 / 只有状态变了 -> `changed=1`。

> 教训：**跨模块复制的"计数语义"迟早会分叉。**
> 这次没靠"记得改另一处"，而是让两处都调用同一个 `write_label_if_changed()`。

### 10.23 两个"后端算了、前端没用"的静默失效（选路页实测）

选路页交出去之前逐条对接口，抓到两处**不报错、只是不对**的问题 ——
它们都属于同一类：**后端把话说清楚了，前端没接**。

**① 每帧的 `flags` 前端一个字都没显示。**
后端辛辛苦苦算出 `position_guessed`（"这帧的框是我**猜**的，不是检测到的"）、
`too_few`（少于最小选项数）、`no_selected`（没标出选中的），
结果页面只显示了 `mismatchBadge` 那一处 —— **flags 全看不见**。
后果很具体：人按了「空格 = 本帧没问题」，后端在图标不够时会**往右补一个框**，
而页面上那个补出来的框和检测到的框长得一模一样 ✗
—— 于是"猜出来的路标"会被当成真路标写进训练集。
现在：每帧底下有彩色小标签，标题也写清了 `crop_rect` 那类契约。

**② `vision.choice_min_options` / `choice_max_options` 是死旋钮。**
配置里有这两项、文档也提了，但**没有任何代码读它**：
训练侧写回写死 `2/4`，运行侧夹取写死 `MIN_OPTIONS`，界面更是把
"2、3、4"三个按钮**硬编码**在 HTML 字符串里。改配置 -> 什么都不变，
而人只会以为"这个值不适合我" ✗。

修法是**一处定义、三处引用**（`train.choice.option_range()`）：

```
配置 -> option_range() -> ① 训练侧写回夹取  ② 运行侧 clamp_answers（fold）
                       -> ③ 界面按钮（接口返回 options.min/max，前端按它长按钮）
```

顺手把"自动默认答案"的口径也对齐了运行时：**检测到少于 min 个图标就按
「没有选路」算**（`video.scan_fine` 里就是 `len(found) >= 2` 才算岔路口），
不再凑数补框。

> 教训：**"配置项存在"不等于"配置项生效"。** 一个没人读的配置比没有更坏 ——
> 它让人以为自己调过了。凡是配置里的旋钮，都要有测试证明"改了真的会变"。

### 10.24 数据里 59 帧「只检测到 1 个图标」——**这是口径问题，得人来定**

`datasets/choice` 的分类统计（由启发式预标注生成的**初稿**）：

```
with_choice 264 / 1200；by_count {1: 59, 2: 138, 3: 52, 4: 15}；selected {0: 64, ...}
```

* `count=1` 的 59 帧：按用户规则（**至少 2 个才算岔路口**）它们**不是**岔路口，
  但预标注把它们写成了"有 1 个选项"（1 个框）；
* `selected=0` 的 64 帧：一个蓝高亮的都没测到 —— 可能是"选中态"没测出来，
  也可能是这帧确实在路标出现前/后。

这两类都是**启发式的初稿**，不是答案。要么人在页面上改掉，要么明确口径后
让预标注按口径写。工具已经把两类都标出来了（`too_few` / `no_selected`，
筛选框选「答案和检测不一致」就能只看这些），**不替用户决定**。

### 10.25 ✗✗ 事故复盘：按键那页把**选路标注写坏**了（42~64 帧）

用户问「**551 个框的位置和当前标定对不上**是什么意思」。查下去发现两层问题：

**第一层：体检拿按键的尺子量选路数据集（假警报）。**
`audit()` 到处硬编码 `labels.CLASSES`（刹车/氮气），于是选路标注的第 0 类
`choice_icon` 被读成"刹车按下"，每个框都被拿去和两个**按键框**比位置：
`346（读成刹车）+ 205（读成氮气）= 551` = 选路数据集的图标框总数 ✓。
数据本身一个字节都没错，报的却是红色 error ✗。
修法：`audit._task_of(ds)` 从 `dataset.json`/`data.yaml` 认出任务，
选路改用**自己的判据**（框在不在选路带里、圆框方不方、用 `choice.prelabel` 重算），
按键那套（对称性、落在两个按键框上）**根本不跑**。

**第二层（这才是真事）：用户在 `/dataset` 上把数据集切到 `choice` 了。**
`dataset.json` 的 `corrections` 记录把时间线钉得很死：

```
17:30:50  corrections: 18 帧（+ last_marks 6 帧）
17:31:43  corrections: 24 帧
```

那一页只有「刹车按下 / 氮气按下」两个按钮，`review.apply_corrections()`
写的是 `Box(CLASS_IDS[c], *cues.BRAKE_KEY_BOX)` —— 于是
**选路的 `labels/train/*.txt` 里被写进了 `[137,497,110,110]` 这种刹车框**，
meta 里却还留着选路的三个答案。界面上**完全看不出来**（框画在左下角，
放大图裁的是上方带区，正好都看不见 ✗✗）。是体检的"框不在选路带里"
把它抓出来的 —— 也就是说，**那个看起来像假警报的检查，顺手抓到了一个真事故**。

修法（三道）：

1. `review._require_keys_dataset()`：`make_review` / `apply_corrections` /
   `mark_reviewed` / `undo_last_mark` 以及 `/dataset` 的逐帧浏览，
   发现数据集任务不是 `keys` 就**直接拒绝**，并说清"选路请去 `/choice`"；
2. `/dataset` 的数据集下拉框里把选路数据集**标出来**（`← 选路，用 /choice 页`），
   别让人先选错（`datasets()` 现在带回 `task`）；
3. 数据修复：把那批帧的选路标注**按同一张图重新预标注**（结果和
   `choice-build` 当初写的完全一致），`status` 回到 `prelabel`
   —— 那次"复核"是误操作，**不能算人复核过**（`train eval` 拿"已复核帧"当真答案）。
   修复脚本 `worktmp/probe/repair_choice_labels.py`，出事前的 meta 备份在
   `worktmp/incident_20260915/`。

> ⚠️ 修的时候还自己踩了一个坑，值得记下来：修复脚本一开始用
> `"conf" in box` 当"这是按键格式"的判据 —— 而**修复脚本自己写的框也带 `conf`**，
> 于是第二遍运行时把上一遍的成果又当坏数据"修"了一遍 ✗。
> 教训：**判据不要用自己会写出来的特征**；现在改成"只看类名 + 框在不在带区"，
> 而且修完把那份多余的 `boxes` 字段删掉（`build_from` 本来就不写它 ——
> 选路的框以**标注文件**为准，多存一份就是第二个"真相"）。

> 教训（和 §10.19 是一对）：**多任务共用一套 UI/函数时，"任务"必须是一等公民。**
> 类别、判据、写回格式、页面，全都要跟着 `task` 走；
> 只要有一处按"反正只有一个任务"写死，早晚会把另一个任务的数据写坏，
> 而且**坏数据往往长得像正常数据**（框画在看不见的地方、计数照常涨）。

### 10.26 ✗✗ 选路页的叠加层：**多除了一个 scale**，圈又大又偏（用户截图那堆大黄圈）

用户原话：「标定界面为什么圆框很乱？而且加一个框和删掉一个框都不准」。
截图里是 3 个套在一起的大黄圈 + 一个偏到第 3 个路标上的"选中"框，而答案是"选第 2 个"。

**根因（`bandPct` / `bandOverlay`）**：

```js
// 错：分母是"整帧像素 → 图内像素"的比例，等于又乘了一遍 zoom
var l = (b[0] - crop[0]) / (cw * scale) * 100;
var w = b[2]        / (cw * scale) * 100;
```

`scale = cropScale(...) = cw / naturalWidth = 1 / zoom`，所以 `cw * scale = cw / zoom`
—— 整个百分比被放大了 `zoom` 倍（默认 2）：**框大了一倍，还从裁图左上角往外推了一倍**
（离左边越远偏得越多，最多偏两三个路标位）。这就是"以为选了第 2 个、红框却画在第 3 个上"。
正确写法是**只除以 crop 的宽高**（`zoom` / 显示尺寸都不该出现）：

```js
var l = (b[0] - crop[0]) / cw * 100;   // 百分比是"占 crop 的几成"，与分辨率无关
```

同一类错误还有两处，一起收拾了：

1. **整帧缩略图**（`fullOverlay`）：宽高写成 `b[2] * (cw/scale/W)` = 真值的 **~10 倍**
   （实测 45.3% vs 应该是 4.53%），于是大圈还糊在下面那张图上；
   而且 `.ch-thumb` 带着 `object-fit: contain` + `max-height: 190px`，
   图在盒子里留黑边 —— 叠加层按"占整帧几成"画，**黑边一出现框就整体错位**。
   现在缩略图 `width: 100%`（盒子 == 图），框宽高就是 `b[2]/W*100`，一个多余系数都没有；
2. **画的是服务器那份框**（`row.boxes`），不是**当前答案**里的框（`ans.boxes`）：
   点图加/删之后画面上**完全看不出来**（要刷新才变），这也是"不准"的一半。

**为什么"加/删一个框不准"还有第三个原因**：界面上说的「第 k 个」是**从左到右**，
而后端点图加的框是 `push` 到数组末尾的 —— 顺序不一致时，红框指着一个圆、
落进标注的 `choice_selected` 却是另一个圆。现在 `setAns()` 每次都按中心 x 重排
（`sortBoxes`），和 `boxes_from_answers()` 的口径一致。

**顺带把点击做准了**：band 图在卡片里只有 ~400 CSS px 宽（整条带 500 整帧像素），
1 CSS px ≈ 1.2 整帧像素，手点最多只能准到 1~2 像素，但"加/删一个框"的真实意图
几乎总是"**这个路标**漏了/多报了"。所以点在检测到的圆附近（默认 1 个半径内）
就直接用**那个圆的精确外接框**（`nearestIcon()`），并且 toast 里写明
"加了检测到的那一个圆" / "附近没有检测到的圆，所以放在你点的位置"。

> **验证方式**（这次特意做成了"能看见"的）：`worktmp/probe/choice_geometry_check.js`
> 把 `bandPct`/`boxPct` 从 `choice.js` 里抠出来真跑，验的不变量是
> **"画出来的百分比 → 换回整帧像素 → 必须回到原位置（<0.5px）"**，
> 并且拿老写法当反例（老写法偏 152px、框宽 116px vs 真值 58px）。
> 再把真帧 + JS 算出的百分比交给 `choice_overlay_render.py` 画成 PNG 肉眼看
> —— 圈正好套在路标上 ✓（存 `worktmp/probe/overlay_render/`）。

> 教训：**百分比布局和"像素比例"是两套坐标系，别混。**
> 一个元素只要用 `%` 定位在"就是那张图"的盒子里，所有 `zoom`/`scale`/`naturalWidth`
> 都该在约分里消掉 —— 它们一旦出现在分母里，就是 bug。
> 另外：**几何改动一定要有"往返"测试**（画出来再量回去），
> 光看代码"觉得对"是不够的 —— 这个 bug 当初看着挺合理，还带着一条自信的注释。

### 10.27 ✗✗ `--run-name` 默认写死 `"keys"`：选路的权重会写进按键的目录

用户让"开始训练选路模型"时实测踩到。`train run --name choice` 打出来的第一行是：

```
开始训练：... -> D:\Projects\a9route\models\runs\keys
```

因为 `--run-name` 的 argparse 默认值是**写死的 `"keys"`**，而它被三处用：

| 用处 | 后果 |
|---|---|
| `train run`：`project=models/runs`, `name=<run_name>` | 选路的训练日志/权重写进 `models/runs/keys/` |
| `train eval`：默认权重 `models/runs/<run_name>/weights/best.pt` | 评的是**按键**的权重 |
| `train export`：同一路径 | **把按键的模型导出成 `models/choice.onnx`** ✗✗ |

最后一条最坏：`choice.json` 清单里会写着选路的两类，而权重是按键的 ——
`train use` 的类序校验**也拦不住**（清单是这次导出的，自洽），
运行时会拿着一个"类别对得上、但根本不认路标"的模型去识别选路，
**不报错、只是识别不出来**。这和 §10.25（`/dataset` 写坏选路标注）是**同一个形状**：
**多任务共用一处写死的默认值**。

修法：`--run-name` 默认改成 `None`，在 `cmd_train` 里用 `_run_name(raw, name)` 解析 ——
**显式给了就听显式的，否则跟数据集同名**（`choice` -> `runs/choice`）。
测试里钉住了四种组合（`run --name choice` / `run` / `export --name choice` /
`run --name choice --run-name abc`）。

> 副作用（已记录、未修）：那次失败的在建训练**覆盖了 `models/runs/keys/args.yaml`**
> （只是这次运行的参数记录，权重 `best.pt` 时间戳未变、没被动）。
> `models/keys.onnx` + `models/keys.json` 是运行时真正用的东西，一直是好的 ✓。

> 教训：**"产物目录/文件名"也是默认值，也会串台。**
> 凡是"任务相关"的名字（数据集、run 名、模型文件名、页面），
> 都只能有**一个来源**（这里是数据集名），别在 argparse 里再写一份常量。

### 10.28 `resolve_backend()` 读的是**磁盘**配置 —— `--set` 的覆盖被无声吃掉

给选路模型做"整片对比"（同一条录像，一遍用模型、一遍用启发式）时踩到：

```
跑 model    ：vision.choice_backend=auto      -> 选路后端: onnx   ✓
跑 heuristic：vision.choice_backend=heuristic -> 选路后端: onnx   ✗✗
```

两次跑的都是**同一个后端**，差点得出"模型和启发式结果完全一样"的错误结论
（那条"一模一样"的路线其实毫无信息量）。

根因在**两层**，都是"读取了配置的另一份来源"：

1. 我的对比脚本先 `cfgmod.apply({...})` 再调 `analysis.analyze()` ——
   而 `analyze()` 一进来就 `load_config()` + `apply()`（按磁盘那份），
   把外面的覆盖**整个盖掉**。这个是我的写法错（正确姿势是
   `analyze(overrides={...})`，CLI 的 `--set` 也是走它）；
2. 但**即使**用对了 `overrides=`，覆盖依然不管用：
   `vision.keys.resolve_backend()` / `vision.choice.resolve_backend()`
   不传 `cfg` 时自己去 **`load_config()` 读磁盘文件** ——
   模块常量那一层确实被灌了新值（阈值都生效），可"用哪个后端/哪个模型"
   是重建时**另读一次磁盘**决定的。于是：
   **`a9route analyze --set vision__choice_backend=heuristic` 是无效的** ✗✗
   （同理 `--set vision__key_model=...`）。

修法：`config.apply(cfg)` 把这次用的那份记在 `_CURRENT` 里，
新增 `config.current()`（= 最近一次 apply 的那份；没 apply 过才退回读磁盘），
两个 `resolve_backend()`/`build_*_detector()` 改用它。测试钉住：
`apply({"choice_backend": "ultralytics"})` 之后 `resolve_backend()` 必须回
`ultralytics`（磁盘默认是 `auto`+不存在的模型 -> heuristic，两者**可区分**，
所以这条测试真的能抓住回归）。

> 教训：**"配置"必须只有一个来源。**
> 同一个进程里同时存在"磁盘上的配置"和"本次生效的配置"时，
> 任何一处自己 `load_config()` 都会让覆盖**静默失效** ——
> 而且失效的往往正是"我要临时换一下试试"的那一项，最容易被误读成"换了没用"。

**同一族的第三处**：`train eval` 的 `--conf` 默认值是 argparse 里**写死的 0.40**，
而运行时用的是 `vision.choice_conf`（实测调成了 0.30）—— 于是评估报的是
**运行时根本不会出现的那套数字**（0.9875，而运行时其实是 0.990）✗。
现在 `--conf` 默认 `None`，解析时取**当前生效**的 `{task}_conf`，
显式给了才用显式的。测试钉住"默认不是写死的常量"。

### 10.29 选路模型训完了：**整片路线和启发式一样**，但"有没有岔路口"好得多

数据：`choice` 1200 帧（**全部人工复核过**，196 帧有岔路口）。
训练：yolov8n / 60 轮 / 10.3 分钟 / imgsz 640。
`box_map50 = 0.9843`（`choice_icon` 0.984、`choice_selected` 0.984），
导出 `models/choice.onnx` + 清单。**但 mAP 不算数**，看的是三个答案：

| conf | 是否有选路 | 有几个选项 | 选第几个 | **三个同时都对** |
|---|---|---|---|---|
| 0.25 | **0.998** | 0.961 | 0.961 | **0.990** |
| 0.30 | **0.998** | 0.961 | 0.961 | **0.990** |
| 0.35 | 0.995 | **0.974** | 0.947 | 0.990 |
| 0.45 | 0.995 | 0.947 | 0.934 | 0.9875 |
| **启发式** | 0.953 | 0.974 | 0.961 | **0.9425** |

* **"是否有选路"是模型赢的地方**：错误 19 帧 -> **1 帧**（0.953 -> 0.998）——
  这正是当初启发式最烦人的毛病（屏幕上任何圆形装饰都会多报一个岔路口）；
* "有几个选项""选第几个"在 76 个真岔路口里**互有胜负、差 1~3 帧**（噪声级）；
* 合成口径（三个答案同时都对）**0.990 vs 0.9425** —— 这才是路线真正吃的东西，
  所以切模型是有依据的（`vision.choice_conf` 取 **0.30**，两档并列最好）。

**整片对比**（`跑图转路线测试.mp4`，模型 vs 启发式各跑一遍）：

```
内容完全一样：13 条选路，值相同（32/31/22/21/…）
差异只有两处 ±1% 的时间点：56%↔57%、82%↔83%（稳定闸多/少一帧的边界效应）
```

也就是说：**这条录像上两条路线等价**（好在没有变坏），模型的优势体现在
"少报假岔路口"上 —— 那是换个录像、或者录像更长时才会兑现的收益。
评估报告现在**两边都给"三个答案同时都对"**，模型不如启发式时会直接写
"**先别切**"（见 `format_choice_eval`）。

### 10.30 ✗✗ 「现在是全用模型了吗」——数了一遍才发现：**粗扫还在跑启发式，而且能一票否掉模型**

用户问"现在识别路线也是用模型了吗"。日志里两行都写着 `onnx`，但**日志只是声明**，
于是写了个计数器（`worktmp/probe/who_decides_route.py`）把两个后端的 `read()`
包起来真跑一遍录像 —— 结论是"大致是，但有一处不是"：

```
按键后端 read()        : 2072
选路后端 read()        : 996
经 reader 转交选路后端 : 996
**落到 HoughCircles**  : 35      ← cues.py:230 _percent_and_icons
裸 RaceReader（没注入）: 1 / 2
```

**两件事**：

1. `CueDetector._percent_and_icons()` 自己造了一个**裸 `RaceReader()`**
   （`cues.py:230`）—— 于是粗扫的 `cue.icons` **一直是 HoughCircles**，
   而细扫早就换成模型了 ✗。白跑一遍启发式只是小账；
2. 大账是：`cue.icons` 被拿去**交叉校验**（`video.py`：
   "选路必须在粗扫也看到 ≥2 个路标才算"）—— 也就是说
   **启发式能一票否决模型判出来的选路**，而"少报假岔路口"正是模型赢的地方，
   这一票把它抹掉了 ✗✗（那条校验是 2026-09-13 为了压"开头多报一次选路"加的，
   当时两边都是启发式，所以合理；现在两边不同源了，它就变成了错的东西）。

修法：

* `CueDetector(choice=...)`：把选路后端**注入进去**，`RaceReader(choice=...)`
  用它造 —— 而且**必须等两个后端都造好之后**再建 `CueDetector`
  （reader 是惰性创建的，先建对象后补后端会被无声忽略）；
* 同时给 `reader` 和 `choice` **直接报错**（否则"以为注入生效了"再演一遍）；
* 交叉校验抽成纯函数 `_cross_check_choices()`，返回 `(留下的, 丢掉的)`，
  调用方把"丢了哪些"**打出来**（不许静默丢 —— 否则"模型少报一次"到底是模型的错
   还是被这一票否掉的，永远查不出来）；
* 顺手：`RaceReader.read(with_icons=False)`，`video.sample_video()`（拼时间轴用，
  根本不需要图标）不再白跑一遍路标检测。

修完再数一遍：**落到 HoughCircles 0 次、裸 reader 0 个**，路线一字未变
（`3,32,5,31,7,360,9,N:0:2:166`）—— 说明这次修的是"谁说了算"，不是"结果"。

> 教训：**"日志说用了什么"和"实际谁在做决定"是两件事，必须能分别验证。**
> 一个后端有没有被真正调用、有没有被别的判据否掉，只靠读代码是看不出来的 ——
> 这次是靠**给每个后端装计数器跑真录像**才定位到 `cues.py:230` 那一行。
> 另外：**跨模块的"否决权"最危险** —— 它不报错、不改数字，只是让结论消失。

### 10.31 「判定全交给模型」：把决策路径上剩下的启发式一条条拔掉

用户看完 §10.30 之后给了明确口径：**"我不需要再用启发式判断有几条路可选或者别的任何操作，
将所有判定都交给 yolo 即可。"** 于是做了一次"决策路径审计"，把还能左右输出的东西全列出来，
逐条处理：

| 残留 | 性质 | 处理 |
|---|---|---|
| `auto` 后端在模型缺失时**静默退回启发式** | 最危险：删掉模型文件后判据无声退化 | 改成**报错**（`auto` = 必须用模型），并把错误变成分析的 **blocker**（CLI 红字+退出码 1、界面红框）。想用启发式只能**显式**设 `heuristic`（调试/预标注），而且日志会写"本次不是模型判的" |
| 「窗口汇总」估算那条老路径（氮气槽青 / 「漂移NN米」/「完成360」文字 → 拼一条路线） | 看着像路线、其实是另一套判据，还会被当成真值 | `suggest_route(..., fine_scan_ran=False)` 时**直接拒绝出路线**并说清怎么修；函数体挪到 `_suggest_route_legacy_window()` 只作存档 |
| `cue.drifting` 叠了 OCR 文字那一路（「漂移」） | 有模型时等于在模型之外多一条判定 | 有按键模型时**只认模型**（漂移 = 按住刹车） |
| 「关自动驾驶 `S:2000`」从 OCR 文字「TOUCHDRIVE」判、而且会**漏进路线** | 唯一一条进路线的非模型判定 | 先摘掉、**再按用户口径放回来**：用户说"touchdrive 还是用 ocr 识别" —— 所以它保留在路线里，但**必须在正文里标明它来自 OCR**（单独一段列出），而且只许 `auto` 这一类从粗扫漏进路线（`drift/spin` 等一律不许）。测试钉住这三点 |
| `sample_video()`（只拼时间轴）每帧白跑一遍选路检测 | 浪费 + 可能跑启发式 | 新增 `with_icons=False`，那条路不再数路标 |

**顺带暴露的一件事**：改完 `auto` 之后 `test_webvideo` 立刻红了，追下去发现
——那些测试**一直在依赖"auto 静默退回启发式"**：测试环境的配置只写在内存里
（`c.apply({...})`），而 `analysis.analyze()` 进来就 `load_config()` + `apply()`
把内存改动**盖掉**（`CONFIG_FILE` 指向一个不存在的文件 → 回到默认 `auto`）。
以前 `auto` 会兜底成启发式，所以看不出来；现在必须**把测试配置写成真文件**
（`tests/__init__.py` 里写一个临时 `config.json` 并让 `CONFIG_FILE` 指过去），
任何一次 `load_config()` 都读到同一份。

还有一个更值钱的发现：`test_webvideo` 的"路线非空"断言，之前是靠**假视频里
根本不存在的操作**撑着的（那个假视频只有百分比读数，却断言路线非空）——
它靠旧兜底路径从窗口汇总/圆检测里凑出了"6 条操作"✗。现在假视频里**真的画上**
刹车/氮气帧，测的才是真那条路。

> 教训：**"兜底"会伪装成"功能正常"。** 一个静默 fallback 存在越久，
> 越会有测试/流程悄悄依赖它；把它换成"报错"时，红掉的测试**未必是坏事** ——
> 那正是它在告诉你"我原来靠的是兜底"。

### 10.32 「服务窗口拒绝连接」：不是崩，是**进程被人结束**（外加一个文档骗人）

用户报"这个窗口拒绝连接了"。查日志：**没有任何 Python traceback**，
最后几行还是正常的 `GET /media/... 200`；后台任务给出的退出码是
`1073807364` = `0x40010004` = **DBG_TERMINATE_PROCESS（被强制结束）**
—— 也就是说**不是程序崩了，是被谁杀了** ✗。

谁杀的：**从 AI 会话里起的进程，会在那条命令/任务结束时被连整棵进程树一起结束**。
两次都吻合：一次是后台任务被回收（那次跑了约 15 小时，直到被回收），
一次是我用 `Start-Process` 起的"独立"进程 —— 它照样挂在同一条命令的进程树里，
那条命令一返回就被清掉（中间它还正常服务了浏览器的几屏请求）。
想用 `schtasks` 绕出去也被环境挡了。

**结论（也是用户那句"服务不应该是在 main 函数运行的时候启动吗"的答案）**：
服务**确实**由 `main()` 启动 —— `main.py` → `a9route.cli.main()` →
`cmd_serve()` → `web.app.main()` → `app.run()`：**一个进程、阻塞**，
Ctrl+C 或关掉那个终端就结束。所以**要长期跑的服务就在自己的终端里起**，
别指望别人（尤其 AI 会话）替你把它养成常驻进程。
为此加了根目录的 `serve.cmd`（双击即起，文件里有说明）。

**顺手修掉一个"文档骗人"**：`main.py` 顶部的用法写着

```
python main.py                  # 起本地 Web 窗口（默认 http://127.0.0.1:8790/）
```

而 `main()` 里是 `if not func: parser.print_help()` —— **不带参数其实只打一屏帮助**
✗✗（用户按文档跑，看到的是帮助，然后去浏览器发现"拒绝连接"，两头都对不上）。
现在 `main()` 在不带参数时**默认 `serve`**，和文档/README 的说法一致；
要看帮助用 `--help`。测试里用假的 `web.app.main` 钉住"不带参数 -> 起窗口"，
**不会真的起服务器**。

> 教训：**"跑起来没反应"和"跑起来但服务不在"是两回事。**
> 先看退出码和日志：有 traceback = 崩了（查代码）；干净结束 + 非 0 退出码 = 被杀了（查进程归属）。
> 另外：**文档承诺的行为要有测试** —— 否则它只是一段话，不是约定。

### 10.33 选路改成**按"选路段"取结果**（用户 2026-09-15 的口径）

用户给的规则（原话意思）：

> 持续没有检测到选路 -> **选路结束状态**；在结束状态下第一次检测到新结果 ->
> **选路开始状态**，并以这一次的结果开始判断"有没有变化"：
> ① 段内**数量、选中都没变** -> 取**第一次**的结果和百分比；
> ② 段内**数量没变、只有选中变了** -> 取**最后一次变化**时的结果和百分比；
> ③ 段内**数量变了** -> **立刻**结束这一段，并进入**新的一段**。

**改之前**是"按每次变化取一条"（`21 -> 22` 就报一条），所以同一段里改了几次选中，
路线里就会出现几条选路 —— 用户要的是"一段选路只留一个结论"。

实现：`core.intent.classify_choices()` 变成一个小状态机（`_stable_segments()` 先做
"连续 `min_hold` 帧同值才认"的闪烁过滤，状态机跑在过滤后的段上）。
两个阈值分工明确：

| 参数 | 作用 | 默认 |
|---|---|---|
| `scan.choice_min_hold` | **闪烁闸**：同一个值要连续这么多采样点才认 | 4（≈0.27s） |
| `scan.choice_idle_hold` | **选路结束闸**：连续这么多采样点没检测到路标 = 这一段结束 | 8（≈0.53s） |

⚠️ 实现里踩到一个细节：一开始把"空档"也只按**过了闪烁闸的段**来数，于是
**比 `min_hold` 短的空档永远数不到**，`idle_hold` 根本调不动（测试当场抓到：
`idle_hold=2` 和默认值给出同一个结果 ✗）。改成**按原始采样点**数空档
（只有"确认到的值"才把它清零），两个阈值才真正各管一段。

**实测效果**（`跑图转路线测试.mp4`，选路从 **12 条 → 7 条**）：

```
改前： 3,32  5,31 ... 40,22  42,21 ... 47,22 ... 50,21 ... 65,32  68,31  69,21 ... 81,22  83,21
改后：        5,31 ...          42,21 ...              50,21  56,22        68,31  69,21 ...        83,21
       └─ 数量没变、选中变了 -> 合并取最后一次 ─┘                └─ 数量 3->2 -> 立刻分两段 ─┘
```

每一条的**判据说明都写进路线注释**了，例如
`这一段里只有「选中」变过，最后一次是 3 选 1 -> 取最后一次变化时`、
`这一段一直没变（2 选 1） -> 取第一次识别到时`，
以及分段的两种原因（`之后持续没检测到路标` / `选项数变了：3 -> 2`）——
哪一条是按哪个规则来的，核对时一眼可见。

> 教训：**规则的"粒度"本身就是需求。** "按变化取"和"按段取"是两种完全不同的语义，
> 而代码里长得几乎一样（都是遍历时间轴）—— 所以这种改动**必须拿真录像前后各跑一遍**
> 并把结果并排看（上面那张 12 → 7 的表就是），光看单测绿了不足以说明"改对了"。

### 10.34 阈值放到 Web 端调（滑块 + 数字框）—— 以及"调了没反应"这一类误导

用户："阈值在 web 端可调，最好有个滑块或者输入框"。
原来那个「参数」弹窗是**只读的文本 dump**（`showParams()` 拼字符串），所以做成了可编辑的：

* 后端 `web/config_ui.py`：一张**参数表**（29 项，按「选路 / 按键 / 信号形状 / 扫描节奏 / 标定框」
  分组），每项带中文名、`min/max/step`、内置默认值、一句说明；
* `GET /api/config` 出表 + 当前值 + 默认值 + 哪些改过 + **当前后端**；
  `POST /api/config` 改值（`{"set":{...}}`）或恢复默认（`{"reset":[...]|"all"}`），
  **落盘 config.json** 并 `apply()`，下一次分析立刻生效；
* 前端：每项一个滑块 + 数字框（互相联动）+「默认」按钮，改过的行高亮，
  顶部「保存到 config.json / 放弃改动 / 全部恢复默认」，下面留一份只读完整配置。

**三条设计上的讲究**（都是踩过同族的坑才加的）：

1. **校验必须在后端**：前端能绕过。范围/类型/未知键都在后端拦，
   而且**有一项不合格就一项都不写** —— 不留半套配置（那种状态最难查）。
2. **⚠️ "这一项在当前后端下生效吗"要标出来**：最坑的一类误导是"调了没反应"——
   比如现在跑的是模型，而 `vision.choice_param2` 只影响 HoughCircles 那条启发式路。
   接口把当前后端对照进每一项（`applies` / `note`），页面直接写
   "当前 choice 用的是**模型**，这一项（启发式判据）不生效" ✓
   （实测 29 项里有 7 项在模型后端下不生效，全被标出来了）。
3. **恢复默认 = 把键从 config.json 里删掉**，而不是把默认值写进去：
   `config.json` 只记**真正的覆盖**，人打开它看到的才是有意义的差异
   （否则文件越写越长，`config list --changed` 全是噪音）。

**验证时我自己踩了两个"预期写错"**（都不是代码的错，是断言的错）：

* 拿 `choice_idle_hold`（选路结束闸）当"改了就该变"的观测点 ——
  结果 2 和 8 给出**同一条路线**。那不是 bug：这条录像里每段选路之间的空档
  **全都 ≥8 个采样点**，所以调小阈值本来就没有东西可拆 ✓。
  换成 `scan.every`（粗扫间隔）这种**确定可观测**的量才对；
* 以为"间隔翻倍 -> 截图减半"：实测 0.5 秒一拍只少 6 张（94 vs 100）——
  因为**百分点截图数是被"百分比个数"（≈100）限住的**，不是被采样率。
  用 2.0 秒才看出差异（26 vs 100）✓。

> 教训：**做"参数真的生效了"的验证，要挑一个"按机制必然变"的观测点。**
> 挑一个"看起来该变"的量，会把**正确行为判成失败**，
> 或者更糟 —— 为了让测试变绿去改本来对的代码。
